// appsettings.json is loaded first. This file may overwrite single values.
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
      // Information is default. ...AspNetCore=Warning suppresses HTTP server logging.
    }
  }
}
